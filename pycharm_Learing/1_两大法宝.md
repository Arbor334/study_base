## dir() 函数

> 打开函数，能让我们知道工具箱以及工具箱中的分割区有什么东西
> 
> ```python
> dir(torch)
> dir(torch.cuda)
> dir(torch.cuda.is_available)
> ```

## help()函数

> 能让我们知道每个工具是如何使用的，工具的使用方法

```python
help(torch.cuda.is_available) #注意将 is_available()的`()`去掉
```
